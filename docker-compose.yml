version: '3'
services:
 mongo:
  image: mongo
  volumes:
   - /data/mongodb/db:/data/db
  ports:
   - "27017:27017"

 api:
  build: .
  command: node index.js
  volumes:
   - .:/usr/app
   - /usr/app/node_modules
  ports:
   - "3000:3000"
   - "8080:8080"
  links:
   - mongo
